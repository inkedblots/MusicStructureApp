<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="MusicAppTheme." parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="android:colorBackground">@color/my_background_light</item>
        <item name="colorAccent">@color/my_accent_color</item>

    </style>

    <!-- Style for Artist list -->
    <style name="ArtistStyle" tools:keep="@style/ArtistStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/list_item_height</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:padding">16dp</item>
        <item name="android:textAppearance">?android:textAppearanceLarge</item>
    </style>
</resources>
